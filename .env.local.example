# Copy to .env.local for local API development and replace with a stable secret.
# All API instances that sign/verify preflight tokens must share this value.
PREFLIGHT_TOKEN_SECRET=replace-with-a-long-random-local-secret
