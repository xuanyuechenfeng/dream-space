# 造梦空间（Dream Space）文生图平台方案

> 本方案基于 2026-07-30 的[即梦 AI 官方站](https://jimeng.jianying.com/)、实际登录后的文生图界面，以及用户提供的产品截图整理。本阶段仅做产品和技术方案，不进行开发。

## 一、产品定位

**项目中文名称**：造梦空间  
**项目英文名称**：Dream Space

造梦空间是一个参考即梦 AI 核心体验设计的中文文生图平台，只保留三条核心链路：

```text
灵感瀑布流 -> 作品详情 -> 做同款/参考图 -> 生成任务 -> 查看与下载
```
不包含：视频、数字人、音乐、画布、图层编辑、资产库、活动中心、技能市场和社交发布。

建议保持功能流程高度相似，但使用独立品牌、图标、配色、文案和演示图片，不直接复制即梦商标及受版权保护的素材。

## 二、功能需求

### 1. 灵感页

- 图片瀑布流，支持不同宽高比，自适应拼接。
- 顶部分类：推荐、人像、摄影、动漫、插画、设计。
- 关键词搜索，支持提示词、风格、主题搜索。
- 无限滚动、骨架屏、图片懒加载。
- 图片悬停显示作者、点赞数、快捷“做同款”。
- 点击任意图片进入作品详情。
- 后台可配置推荐、热度、分类和排序。

产品不建设用户发布、关注体系和评论系统，灵感内容统一由平台运营和后台配置。

### 2. 灵感详情页

采用沉浸式三栏结构：

- 左侧：极简品牌导航，只有灵感、生成。
- 中间：高清大图，保持原始比例，支持上下切换作品。
- 右侧：作者、作品描述、生成日期、AI 标识、提示词、模型、比例、清晰度、点赞数。
- 操作：点赞、下载、复制提示词、做同款、用作参考图。
- 默认仅展示沉浸式看图详情，不显示生成输入器，以最大化图片展示面积。
- 点击“做同款”后，在当前详情页底部展开输入器，带入提示词、模型、比例和清晰度；点击发送后才进入生成页。
- 点击“用作参考图”后，在当前详情页底部展开输入器，带入当前图片、提示词和参数；参考图可删除，点击发送后才进入生成页。
- 不提供画布编辑，所谓“编辑”仅指修改提示词后重新生成。

### 3. 生成页

页面结构：

- 左侧一级导航：灵感、生成。
- 二级侧栏：新对话、历史会话、会话重命名和删除。
- 主区域：以时间线展示提示词、参考图、参数、进度和生成结果。
- 底部自适应输入器位于页面独立布局行，不覆盖生成历史；空闲时收为单行，聚焦、有提示词或参考图时展开。
- 支持纯文本生成以及“文本 + 参考图”生成。
- 参考图支持上传、删除、预览和排序，建议最多 4 张。
- 参数包括模型、比例、分辨率、生成张数。

生成参数：

| 参数 | 可选值 |
|---|---|
| 模型 | 通用模型、写实模型、动漫模型 |
| 比例 | 智能、21:9、16:9、3:2、4:3、1:1、3:4、2:3、9:16 |
| 清晰度 | 2K、4K，并显示最终输出宽高 |
| 张数 | 1-8 |
| 高级项 | Seed、负面提示词、提示词增强 |

任务状态：排队、生成中、成功、部分成功、失败、已取消。

结果操作：单图预览、下载、再次生成、修改提示词、复制参数、做同款、用作参考图。失败任务显示原因和重试按钮。

### 4. 基础账户能力

账户不是一级业务模块，但需要用于保存数据：

- 手机号或第三方登录。
- 会话历史、点赞记录、生成记录。
- 免费额度和每日生成次数。
- 只提供免费额度和用量控制，不建设充值、会员和支付系统。

## 三、页面路由

```text
/                       自动进入灵感页
/inspiration            灵感瀑布流
/inspiration/:id        作品详情
/generate               新建生成会话
/generate/:sessionId    历史生成会话
/login                  登录
```

## 四、系统架构

采用“前后端分离的模块化单体 + 独立异步 Worker”，复杂度适中，后续容易扩容。

```text
Web 前端
   -> API 服务
      -> PostgreSQL
      -> Redis/BullMQ 任务队列
      -> 对象存储与 CDN
      -> 图片生成 Worker
         -> 模型供应商 API
```

生成流程：

1. API 校验用户身份、幂等键、提示词、参考图、额度和生成前内容安全。
2. 在同一数据库事务中创建任务、记录额度冻结流水并写入任务事件。
3. Worker 从队列获取任务，取得任务租约后调用模型供应商。
4. 对供应商轮询或回调进行验签、去重，并处理超时、重试和取消。
5. 对生成结果进行二次内容审核，审核通过后转存自己的对象存储。
6. 生成缩略图和 WebP/AVIF 版本，清除不需要的 EXIF 等隐私元数据。
7. 通过 SSE 向前端推送带序号的任务事件，支持断线重连和状态补偿。
8. 成功后写入作品记录并结算额度；失败时返还额度并记录可追踪原因。

模型层必须使用适配器接口，避免绑定单一供应商。可优先评估火山引擎 Seedream，同步预留阿里万相、腾讯混元、SiliconFlow、fal.ai 或自建 ComfyUI。即梦网页中的专有模型不等于一定存在可直接调用的公开 API。

供应商正式接入前需要评审服务区域、内容政策、输入数据留存、是否用于训练、输出商用授权、并发配额、价格、服务可用性和退出迁移方案。平台至少配置主供应商和可选备用供应商，并通过后台开关控制模型上下线与流量路由。

## 五、技术选型

- 前端：Next.js 最新稳定版、React、TypeScript。
- UI：Tailwind CSS、Radix UI、Lucide Icons。
- 状态：TanStack Query 管理服务端状态，Zustand 管理输入器临时状态。
- 后端：NestJS、TypeScript、REST API、SSE。
- 数据库：PostgreSQL、Prisma ORM。
- 队列：Redis、BullMQ。
- 文件：兼容 S3 的对象存储，国内可选火山 TOS、阿里 OSS。
- 图片处理：Sharp，生成缩略图、WebP/AVIF 和元数据。
- 鉴权：NestJS 签发短期 Access Token 和可轮换 Refresh Token，通过 `HttpOnly`、`Secure`、`SameSite` Cookie 保存；服务端维护刷新令牌撤销记录。
- 部署：Docker；前端、API、Worker 分容器部署。
- 监控：Sentry、OpenTelemetry、结构化日志、链路追踪、任务耗时、供应商成功率和模型成本指标。
- API 文档：OpenAPI，接口与事件结构进入版本管理。
- 依赖策略：开发启动时锁定具体稳定版本，提交 lockfile，并使用自动化依赖漏洞扫描。

## 六、核心数据模型

- `users`：用户、头像、状态、剩余额度。
- `sessions`：生成会话、标题、更新时间。
- `generation_tasks`：提示词、参考图、模型参数、状态、成本、错误。
- `generated_images`：原图、缩略图、尺寸、Seed、任务关联。
- `inspiration_items`：灵感作品、提示词、分类、热度、展示状态。
- `likes`：用户与灵感作品的点赞关系。
- `quota_ledger`：额度增加、冻结、消费和返还流水。
- `session_messages`：会话中的提示词、参数快照、任务关联和消息顺序。
- `reference_images`：参考图对象、来源、排序、审核状态和生命周期。
- `task_attempts`：每次模型调用、供应商任务号、租约、耗时、成本和错误。
- `task_events`：任务状态事件、事件序号和 SSE 断线补偿依据。
- `model_providers`：供应商、模型、价格、能力、路由权重和启停状态。
- `moderation_records`：提示词、参考图、生成图的审核结果和处置记录。
- `reports_and_appeals`：举报、申诉、处理状态、处理人和证据记录。
- `file_objects`：存储 Key、哈希、MIME、尺寸、归属、引用和删除状态。
- `auth_identities`：登录方式、外部身份和账户绑定关系。
- `refresh_tokens`：刷新令牌哈希、设备、过期、轮换和撤销状态。
- `consent_records`：用户同意的协议版本、时间、来源和撤回记录。
- `admin_users`、`roles`、`permissions`：管理员、角色和权限关系。
- `audit_logs`：管理员和系统高风险操作的不可变审计记录。
- `system_configs`：额度、限流、审核、模型和运营配置的版本化记录。

灵感作品可以来自后台精选生成结果，不必建设完整的社区发布系统。

## 七、运营管理后台

管理后台不出现在普通用户的一级导航中，仅供授权运营和管理员使用。

### 1. 灵感内容管理

- 灵感作品录入、批量导入、编辑、预览、上下架和定时发布。
- 分类、标签、推荐位、排序权重和搜索关键词配置。
- 作品图片、作者展示名、提示词、模型参数和授权凭证维护。
- 热度数据修正、违规内容紧急下架和 CDN 缓存清除。

### 2. 用户与额度管理

- 用户检索、状态查看、封禁、解封和注销处理。
- 免费额度发放、扣减、返还和人工调整，所有调整必须记录原因。
- 会话、任务、登录设备和异常行为查询。
- 单用户并发、日额度和风险等级配置。

### 3. 任务与模型管理

- 生成任务检索、详情、终止、重试和人工补偿。
- 模型上下线、供应商路由、并发额度、价格和降级开关。
- 队列积压、供应商余额、成功率、耗时和单位成本看板。
- 供应商故障时一键停用、切换备用模型或暂停生成入口。

### 4. 审核与申诉管理

- 提示词、参考图、生成图的机审和人工复核队列。
- 举报、申诉、处置、恢复和证据留存流程。
- 敏感词、审核阈值、白名单和黑名单配置。
- 审核员与管理员采用 RBAC，高风险操作需要二次确认并写入审计日志。

## 八、商用合规与内容治理

项目面向中国大陆公众提供服务时，应在上线前由专业法务或合规人员确认以下事项的具体适用范围：

- 完成主体资质、ICP备案或许可、公安备案及必要的网站公示信息。
- 评估生成式人工智能服务、深度合成算法备案和安全评估要求。
- 提供用户协议、隐私政策、AI 功能使用协议、儿童或未成年人保护规则。
- 对网页展示和下载的 AI 生成内容添加符合要求的显式标识与文件隐式标识。
- 明确用户对提示词和参考图拥有必要权利，不得侵害版权、肖像权、商标权和隐私权。
- 明确平台、用户和模型供应商之间的输入数据使用范围及生成结果授权边界。
- 建立违法和不良内容发现、阻断、下架、举报、申诉、证据留存和监管协作流程。
- 灵感作品必须保存来源、授权方式和运营责任人，不使用来源不明的网络图片。
- 对未成年人、公众人物、真人肖像、证件、医疗和其他高风险场景设置专门规则。

内容治理采用多层策略：生成前审核提示词和参考图，生成后审核结果图，进入灵感池前进行人工复核，上线后持续接受举报与巡检。审核拒绝需要提供适当的用户提示，但不得暴露可用于绕过审核的内部规则。

## 九、数据治理与安全策略

安全能力分阶段建设：工程初期建立身份、数据归属和最小权限基础，模型接入后完善审核、风控和供应商安全，上线前完成系统性安全加固和攻防验证。

### 1. 数据生命周期

- 为提示词、参考图、生成图、缩略图、日志、任务事件和审核记录分别定义保存期限。
- 用户删除内容后立即对普通访问不可见，并在约定期限内完成对象存储和 CDN 的物理清理。
- 用户注销时处理会话、图片、身份、令牌和额度数据；依法必须保留的数据进入隔离存储。
- 提供个人数据查询、更正、导出、删除和撤回授权流程。
- 备份数据设置独立过期策略，不能因存在备份而无限期保留已删除内容。
- 上传和生成图片默认清除定位、设备等非必要 EXIF 信息。

建议基线：回收站保留 7 天，主存储在删除申请后 30 天内物理清理，备份最长保留 90 天；最终期限以正式隐私政策和合规评估为准。

### 2. 应用与基础设施安全

- 所有资源接口校验用户归属，后台接口校验角色和具体权限。
- 防护 XSS、CSRF、SQL 注入、SSRF、恶意文件、图片解析漏洞和暴力验证码攻击。
- 上传文件校验扩展名、MIME、文件头、像素数和体积，并重新编码后存储。
- 文件下载使用短期签名地址，私有原图不得暴露永久公共 URL。
- 供应商回调进行签名、时间戳和重放校验。
- 密钥进入专用密钥管理服务，支持轮换，不得进入前端、日志和代码仓库。
- 日志对 Token、手机号、提示词中的敏感信息和存储签名进行脱敏。
- 建立 WAF、接口限流、登录风控、依赖扫描、镜像扫描和安全补丁流程。

## 十、API 契约与任务可靠性

### 1. API 范围

- `/auth`：登录、刷新、退出、账户绑定和注销。
- `/inspirations`：列表、搜索、详情、点赞和做同款参数读取。
- `/sessions`：创建、列表、详情、重命名和删除。
- `/uploads`：上传凭证、上传完成确认、查询和删除。
- `/generation-tasks`：创建、取消、重试、详情和事件订阅。
- `/images`：详情、下载凭证和设置为参考图。
- `/quota`：余额、用量和流水查询。
- `/admin`：运营、审核、任务、用户、模型和系统配置。

所有接口使用统一响应结构、错误码、游标分页和请求追踪 ID。写接口通过 `Idempotency-Key` 防止重复提交；OpenAPI 文档、数据库字段和前端类型需要保持同步。

### 2. 异步任务可靠性

- 创建任务、冻结额度和写入初始事件在一个事务中完成。
- Worker 使用租约和心跳，进程退出后任务能够被其他 Worker 接管。
- 重试使用指数退避和最大次数限制，不可恢复错误直接进入死信队列。
- 供应商回调按供应商任务号和事件 ID 去重，状态只能按合法状态机迁移。
- 任务取消必须区分未调用供应商、供应商可取消和供应商不可取消三种情况。
- 定时对账任务检查数据库、队列、供应商状态、文件结果和额度流水的一致性。
- SSE 事件包含递增序号；客户端重连时通过最后事件序号补发遗漏事件。
- 人工补偿操作必须有权限控制、原因记录和完整审计日志。

## 十一、成本、容量与非功能指标

### 1. 成本与容量控制

- 记录每个模型、分辨率和张数的预估成本与实际成本。
- 配置单用户并发数、每日额度、全局并发、队列长度和供应商调用上限。
- 配置日成本、月成本、供应商余额和异常失败率告警。
- 达到预算或供应商风险阈值时，可以降级模型、限制清晰度、暂停新任务或切换供应商。
- 对原图、缩略图、CDN 流量、日志和备份分别制定存储周期与成本预算。

### 2. 建议服务指标

| 指标 | 上线基线 |
|---|---|
| Web/API 月可用性 | 不低于 99.9% |
| 普通读取接口 P95 | 不高于 500ms，不含模型生成时间 |
| 创建生成任务接口 P95 | 不高于 800ms |
| 任务提交成功率 | 不低于 99.9% |
| 正常负载下排队等待 P95 | 不高于 30 秒 |
| SSE 断线恢复 | 10 秒内自动重连并补齐状态 |
| Web LCP | 常见网络条件下不高于 2.5 秒 |
| Web CLS | 不高于 0.1 |
| 数据库 RPO | 不高于 15 分钟 |
| 核心服务 RTO | 不高于 2 小时 |

正式数值应根据供应商配额、预算和压力测试结果调整。监控需要覆盖请求量、错误率、延迟、队列积压、Worker 存活、供应商成功率、单位生成成本、审核拒绝率和存储增长率。

## 十二、前端样式

整体采用明亮、安静、专业的创作工具风格：

- 背景以白色和浅灰为主，正文近黑色。
- 单一品牌强调色建议使用青绿色或亮蓝色，避免复制即梦渐变标识。
- 左侧导航宽约 `72px`，历史侧栏约 `280px`。
- 顶部与底部生成器使用轻边框和细阴影，圆角不超过 `8px`。
- 灵感页使用无间距或 `2-4px` 间距的密集瀑布流。
- 详情页图片占主要视觉面积，右侧信息栏约 `420-480px`。
- 生成页内容最大宽度约 `960-1120px`，生成器固定在底部。
- 动效只用于图片加载、任务进度、详情切换和菜单反馈。
- 桌面端优先；移动端改为底部导航、单列瀑布流和全屏详情。

## 十三、开发计划

开发按以下 8 个阶段顺序推进。每个阶段通过验收后再进入下一阶段，避免界面、接口和模型调用同时变化。安全与合规采用“前期打基础、后期集中完善”的方式，不阻塞主要产品功能逐步成型。

| 阶段 | 当前状态 |
|---|---|
| 阶段 1：需求固化与交互原型 | 基本完成；390px 移动视口实机复核待办 |
| 阶段 2-8 | 未开始 |

### 阶段 1：需求固化与交互原型

**目标**：确定完整产品边界、页面结构和交互规则。

**主要任务**：

- 输出灵感页、灵感详情页、生成页和登录页的高保真原型。
- 定义导航、搜索、瀑布流、详情切换、做同款、参考图和生成会话流程。
- 补齐空数据、加载、生成中、失败、限额和网络异常状态。
- 确定品牌名称、Logo、字体、颜色、间距和响应式断点。
- 形成页面清单、组件清单、字段字典和验收用例。

**交付物**：产品需求文档、页面原型、视觉规范、交互状态表、验收清单。

**验收条件**：所有用户路径均可在原型中完整走通，不存在未定义的页面跳转和操作状态。

### 阶段 2：工程基础与设计系统

**目标**：搭建可持续开发的前后端工程和公共能力。

**主要任务**：

- 建立前端、API、Worker 的项目结构和环境配置。
- 配置 TypeScript、代码检查、格式化、提交检查和自动化测试。
- 建立颜色、字号、间距、圆角、阴影和响应式 Design Token。
- 实现按钮、输入框、弹窗、菜单、图片容器、骨架屏、Toast 等基础组件。
- 建立数据库迁移、Redis、对象存储和本地开发环境。
- 建立最小账户身份、资源归属、管理员角色、额度账本、幂等键和审计字段，暂不展开完整安全体系。
- 定义统一错误码、游标分页、请求追踪 ID、OpenAPI 和 SSE 事件契约。
- 配置持续集成，自动执行检查、测试和构建。

**交付物**：可运行的工程骨架、组件库、数据库基础结构、开发环境说明。

**验收条件**：新环境可按照文档一键启动；前端、API、Worker、数据库和 Redis 均通过健康检查。

### 阶段 3：灵感模块

**目标**：完成从灵感浏览到作品详情的完整体验。

**主要任务**：

- 实现灵感瀑布流、分类筛选、关键词搜索、懒加载和无限滚动。
- 实现图片骨架屏、加载失败占位、悬停信息和响应式布局。
- 实现沉浸式详情页、大图展示、上下作品切换和元数据区域。
- 实现点赞、下载、复制提示词、做同款和用作参考图。
- 建立灵感列表、详情、搜索和点赞接口。
- 使用数据库种子和受控内部接口准备灵感数据，完整运营后台放在阶段 6 建设。

**交付物**：灵感页、灵感详情页、灵感管理能力及相关接口。

**验收条件**：用户能搜索和浏览作品，进入详情后能正确复制参数，并把提示词或参考图带入生成页。

### 阶段 4：生成交互与会话系统

**目标**：完成生成页全部交互和历史会话能力，暂以模拟任务验证流程。

**主要任务**：

- 实现新对话、历史会话、重命名、删除和最近访问排序。
- 实现提示词输入、参考图上传/预览/删除/排序和参数选择。
- 实现任务时间线以及排队、生成中、成功、部分成功、失败和取消状态。
- 实现结果预览、单图下载、重新生成、修改提示词、复制参数和参考复用。
- 实现会话、生成任务、生成结果和文件上传接口。
- 使用可控的模拟 Worker 验证进度、失败和重试流程。
- 同步编写会话、上传、任务状态机和页面核心交互测试。

**交付物**：完整生成页、会话系统、任务状态机和模拟生成链路。

**验收条件**：不调用真实模型时，所有生成状态和结果操作均可稳定演示，刷新页面后会话与任务状态不丢失。

### 阶段 5：模型接入与异步任务

**目标**：接入真实图片生成模型并保证任务可靠执行。

**主要任务**：

- 定义统一模型适配器，屏蔽不同供应商的请求和返回差异。
- 接入选定的主模型，并配置超时、轮询或回调处理。
- 使用 BullMQ 实现排队、并发限制、重试、取消和失败处理。
- 使用 SSE 推送任务状态和生成进度。
- 将供应商结果转存对象存储，生成缩略图和 WebP/AVIF 版本。
- 记录调用耗时、模型参数、供应商任务号、成本和错误信息。
- 实现供应商不可用时的熔断与降级策略。
- 实现任务租约、心跳、回调验签去重、死信队列和定时一致性对账。
- 接入基础的生成前与生成后内容审核，先保证真实链路可控。
- 实现单用户与全局并发限制、成本统计和预算熔断。

**交付物**：真实文生图链路、模型适配层、异步 Worker、进度推送和结果存储。

**验收条件**：文本生成和参考图生成均可完成；重复回调不会产生重复图片；任务失败能够重试并保持数据一致。

### 阶段 6：运营管理后台

**目标**：让灵感内容、用户、任务、模型和审核流程具备可运营能力。

**主要任务**：

- 实现灵感作品录入、编辑、预览、上下架、分类、推荐和排序。
- 实现用户检索、状态查询、封禁、解封、注销处理和额度调整。
- 实现任务查询、终止、重试、人工补偿和结果下架。
- 实现模型上下线、供应商路由、并发、价格和降级开关。
- 实现机审复核、举报、申诉、处置和证据记录。
- 实现管理员 RBAC、高风险操作二次确认和操作审计。
- 实现请求、任务、供应商、成本、审核和存储运营看板。

**交付物**：运营管理后台、管理员权限体系、审核工作台和运营监控看板。

**验收条件**：运营人员不依赖开发人员即可完成日常内容、用户、任务、模型和审核操作；所有高风险操作均可追踪。

### 阶段 7：账户、安全、数据治理与合规完善

**目标**：在主要业务链路稳定后，系统性完成商用安全与合规能力。

**主要任务**：

- 完成登录、Refresh Token 轮换、退出、设备会话、账户注销和异常登录处理。
- 全面检查普通用户资源归属、后台 RBAC 和接口最小权限。
- 完善额度冻结、消费、返还、并发一致性和人工调整审计。
- 实现数据查询、导出、删除、撤回授权和完整的数据生命周期任务。
- 加固上传、下载、回调、Cookie、密钥、日志脱敏、限流、WAF 和验证码风控。
- 完善提示词、参考图、生成图、灵感入池、举报和申诉的内容治理闭环。
- 增加 AI 生成内容显式标识和文件隐式标识。
- 完成用户协议、隐私政策、AI 功能使用协议和必要的备案/评估材料。
- 开展越权、注入、XSS、CSRF、SSRF、恶意上传、重放和依赖漏洞测试。

**交付物**：账户与安全加固版本、数据治理机制、合规材料、内容治理流程和安全测试报告。

**验收条件**：越权、额度并发、恶意上传和敏感内容测试通过；数据删除可验证；上线所需协议、公示、标识和备案事项均有明确结论。

### 阶段 8：质量验收、性能优化与上线

**目标**：完成生产环境部署和商用上线质量保障。

**主要任务**：

- 补齐单元测试、接口集成测试、视觉回归和核心路径端到端测试。
- 验证 Chrome、Edge、Safari 及桌面端和移动端主要分辨率。
- 完成键盘操作、焦点、对比度、图片替代文本等基础无障碍检查。
- 优化瀑布流虚拟化、图片格式、懒加载、缓存、CDN 和 Core Web Vitals。
- 对生成任务、队列、SSE、数据库、Redis 和对象存储进行压力与故障测试。
- 验证供应商故障、Worker 崩溃、重复回调、网络中断和额度对账场景。
- 配置日志、Sentry、OpenTelemetry、服务告警、成本告警和值班通知。
- 建立产品统计事件和漏斗，覆盖灵感点击、做同款、任务提交、成功和下载。
- 制定数据库备份、对象存储生命周期、故障恢复、回滚和应急响应方案。
- 部署生产环境，进行数据初始化、上线检查、小流量验证和逐步放量。

**交付物**：测试报告、性能与容量报告、部署配置、监控面板、运营分析、运维手册和上线版本。

**验收条件**：核心用户链路端到端通过，商用服务指标达到上线基线，错误和成本可追踪，生产环境具备备份、恢复、降级和回滚能力。

## 十四、整体完成标准

- 用户能够登录并查看自己的生成会话和额度。
- 用户能够浏览、搜索和打开灵感作品详情。
- “做同款”和“用作参考图”能够准确传递提示词、图片和生成参数。
- 用户能够提交文本或参考图生成任务，并实时看到任务状态。
- 生成结果能够预览、下载、重新生成和再次作为参考图使用。
- 刷新页面或重新登录后，会话、任务和结果仍然完整可用。
- 平台不出现视频、画布、资产库、数字人、音乐和社交发布功能。
- 所有模型调用、额度变化和任务错误都有可追踪记录。
- 运营人员能够独立管理灵感内容、用户、任务、模型、审核和系统配置。
- 提示词、参考图、生成图和灵感作品均经过对应的审核与处置流程。
- 用户能够按隐私规则查询、导出和删除个人数据，数据生命周期任务可验证。
- 平台具备明确的可用性、性能、成本、容量、备份、恢复和告警指标。
- AI 内容标识、用户协议、隐私政策和相关备案/评估满足上线地区的适用要求。
