# 贡献规范

## 分支规则

- `main`：稳定、可部署版本，禁止直接提交。
- `feature/<scope>`：新功能。
- `fix/<scope>`：缺陷修复。
- `design/<scope>`：视觉和交互调整。
- `docs/<scope>`：文档调整。
- `chore/<scope>`：工程维护。

分支必须从最新 `main` 创建，一个分支只处理一个明确目标。

## 提交规范

提交信息采用以下格式：

```text
<type>: <改造内容>
```

可用类型：`feat`、`fix`、`design`、`docs`、`refactor`、`test`、`chore`。

示例：

```text
design: 优化深色模式生成参数对比度
fix: 修复跟随系统主题刷新失效
docs: 补充 GitHub Pages 部署说明
```

## PR 流程

1. 同步最新 `main`。
2. 创建独立分支并完成修改。
3. 运行与改动范围匹配的验证。
4. 推送分支并创建 PR。
5. 在 PR 中说明背景、改动、验证结果和视觉影响。
6. 通过自动检查和人工审核后合并。

禁止将访问令牌、密码、Cookie、用户数据、`.env` 文件或其他敏感信息提交到仓库。

## 原型验收

涉及 `prototype/index.html` 的修改至少检查：

- JavaScript 语法无错误。
- 无重复 DOM ID。
- JavaScript 引用的 DOM ID 均存在。
- 浅色、深色与跟随系统主题可用。
- 中文和英文界面无明显遗漏。
- 灵感、详情、生成、会话、登录主流程可操作。
- 桌面和移动视口不存在内容遮挡、越界或不可见文字。
- 图片预览、下载、参考图添加与删除可用。
