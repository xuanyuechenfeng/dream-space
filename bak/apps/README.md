# 应用目录

`apps` 包含四个独立运行和部署的应用。浏览器应用只能通过 API 访问业务数据；API 和 Worker 共享业务规则，但职责不同。

| 目录     | 用途                               |
| -------- | ---------------------------------- |
| `web`    | 普通用户端                         |
| `admin`  | 运营管理端                         |
| `api`    | HTTP、SSE、鉴权和业务编排          |
| `worker` | 队列消费、模型调用、审核和图片处理 |
