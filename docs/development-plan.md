# 开发阶段计划

本文档用于制定阶段目标、记录验收结果并决定是否进入下一阶段。状态只允许使用“未开始、进行中、已完成、阻塞”。

## 阶段总览

| 阶段               | 目标                       | 状态   | 下一阶段准入条件                              |
| ------------------ | -------------------------- | ------ | --------------------------------------------- |
| 0. 产品与原型      | 固化范围、流程和视觉基线   | 已完成 | 原型和阶段 1 文档通过评审                     |
| A1. 目录与文档基线 | 建立可维护的工程边界       | 已完成 | 目录、README、链接和 Git 检查通过             |
| A2. 可运行工程骨架 | 四个应用和本地依赖可启动   | 已完成 | 本地与 PR CI 全部通过，可进入阶段 B           |
| B. 用户端 MVP      | 跑通模拟图片生成闭环       | 已完成 | 完整栈门禁通过，可进入阶段 C                  |
| C. 真实生成能力    | 安全可靠地调用真实模型     | 进行中 | 供应商、审核、重试和可靠性门禁通过；对账为 P2 |
| D. 运营与上线      | 具备管理运营和生产保障能力 | 未开始 | ADM-0 至 ADM-4 及 D1-D6 门禁通过              |

## 计划与实际偏差审计

2026-08-07 完成首次执行审计。功能主线与产品目标一致，但商用保障计划落后于功能实现：阶段 D 原先只有四条粗粒度验收条件，无法覆盖安全、治理、可观测性、灾备和生产发布的独立门禁；部署文档也仍停留在原型静态站点。

本计划已据此将阶段 D 拆成 D1-D6，并将真实供应商阻塞、额度账务模型、多实例对账风险、上线阻断项和关键架构决策纳入 [商用就绪审计与决策记录](commercial-readiness.md)。原计划没有日期和负责人基线，因此不使用主观百分比判断延期；后续以可复核的准入/退出证据评估进度。

## 阶段 A1：目录与文档基线

### 目标

- 建立 `apps/web`、`apps/admin`、`apps/api`、`apps/worker` 的完整目录骨架。
- 建立 `packages/ui`、`packages/contracts`、`packages/core`、`packages/db`、`packages/config`。
- 建立 `infrastructure/docker`、`e2e` 和 `scripts`。
- 让根 README 准确说明当前状态、架构、目录和文档入口。
- 为每个应用、共享包和支持目录明确职责、依赖边界和禁止事项。

### 验收条件

- [x] 规划中的目录全部存在并能被 Git 跟踪。
- [x] 根 README 与项目实际状态一致，不声称未实现的命令可以运行。
- [x] 所有 Markdown 相对链接指向存在的文件。
- [x] 不存在重复数据库目录或职责冲突的共享包。
- [x] `git diff --check` 通过，无凭据和真实用户数据。

### 完成评估

评估日期：2026-08-01

| 检查项         | 结果 | 证据                                                     |
| -------------- | ---- | -------------------------------------------------------- |
| 目录完整性     | 通过 | 四个应用、五个共享包及三个支持目录均存在                 |
| 文档准确性     | 通过 | 根 README 明确区分当前原型和阶段 A2 才提供的正式应用     |
| Markdown 链接  | 通过 | 自动检查 14 份入口及模块 README，无失效相对链接          |
| 职责边界       | 通过 | 数据库目录统一为 `packages/db`，应用与共享包均有边界说明 |
| 基础安全与 Git | 通过 | `git diff --check` 和敏感配置赋值扫描通过，原型无改动    |

阶段结论：目标全部达成，可以进入阶段 A2。当前目录中尚无可运行的正式应用，这是 A1 的预期范围，不属于遗留缺陷。

## 阶段 A2：可运行工程骨架

### 目标

- 配置 pnpm workspace、Turborepo、TypeScript、ESLint、格式化和测试命令。
- 初始化两个 Next.js 应用、NestJS API 和 BullMQ Worker。
- 同时支持 Docker Compose 和 macOS 物理机两种本地依赖启动方式；当前骨架需要 PostgreSQL、Redis，兼容 S3 的对象存储保留在 Docker 配置中供阶段 C 使用。
- 提供 API 健康检查以及四个应用的最小启动页面/进程。
- 建立 CI，对所有工作区执行 lint、typecheck、test 和 build。

### 验收条件

- [x] 新环境按照 README 可以安装依赖并启动全部服务。
- [x] 用户端、管理端和 API 健康检查可访问，Worker 能连接队列。
- [x] 所有基础检查命令在本地和 CI 中通过。
- [x] `.env.example` 完整，仓库中不存在真实密钥。

### 当前评估

评估日期：2026-08-03

| 检查项         | 结果     | 证据                                                                  |
| -------------- | -------- | --------------------------------------------------------------------- |
| 依赖与供应链   | 通过     | pnpm 11 lockfile 校验通过，原生安装脚本使用显式白名单                 |
| lint           | 通过     | Turbo 执行 12 个任务，全部成功                                        |
| TypeScript     | 通过     | Turbo 执行 12 个任务，全部成功                                        |
| 单元测试       | 通过     | 9 个测试文件、11 个测试全部通过                                       |
| 生产构建       | 通过     | 9 个 workspace 构建成功，两个 Next.js 页面完成静态生成                |
| Prisma         | 通过     | Prisma Client 7.9.1 生成成功                                          |
| Docker Compose | 配置通过 | 独立 `docker-compose config --quiet` 通过；本机 Docker daemon 未启动  |
| 本机依赖       | 通过     | Homebrew PostgreSQL 17 接受连接，项目专用 Redis 返回 PONG             |
| 运行时         | 通过     | web/admin 返回 HTTP 200，API 健康检查正常，Worker 成功连接 Redis 队列 |
| 本机脚本       | 通过     | `pnpm check` 包含两个 Shell 启动脚本的语法检查                        |
| 远端 CI        | 通过     | PR #3 的 `CI/verify` 成功，run `30780258935`                          |

| 评估引用     | 值                                   |
| ------------ | ------------------------------------ |
| 分支         | `chore/runnable-monorepo-foundation` |
| 实现提交     | `3fe0838`                            |
| Pull Request | `#3`                                 |

阶段结论：所有验收条件均已达成，物理机启动路径已实测，PR CI 通过。阶段 A2 标记为“已完成”，具备进入阶段 B 的工程准入条件；PR 合并仍需用户明确批准。

## 阶段 B：用户端 MVP

### 目标

跑通以下最小闭环：

```text
灵感列表 -> 灵感详情 -> 登录 -> 创建会话 -> 提交任务
-> Worker 模拟生成 -> SSE 返回进度 -> 展示结果 -> 下载
```

管理端同期只实现登录、灵感管理和任务查询。模型结果先使用可重复的模拟数据，避免真实供应商接入阻塞业务开发。

### 内部里程碑

| 里程碑 | 可验证目标                                                   | 状态   | 下一项准入条件                                         |
| ------ | ------------------------------------------------------------ | ------ | ------------------------------------------------------ |
| B1     | PostgreSQL 灵感数据、公开列表/详情 API、响应式用户端灵感目录 | 已完成 | 52 条基线数据可查询，API/页面/主题/语言/响应式检查通过 |
| B2     | 手机验证码演示登录、安全会话、协议确认和登录意图恢复         | 已完成 | 登录集成测试和跨刷新意图恢复 E2E 通过                  |
| B3     | 会话、任务、额度数据模型，提交 API、BullMQ 模拟生成和 SSE    | 已完成 | 状态机、幂等、队列和 SSE 断线恢复测试通过              |
| B4     | 生成工作台、会话时间线、结果预览下载及失败恢复               | 已完成 | 用户端核心生成流程 E2E 和远端 CI 通过                  |
| B5     | 管理端登录、灵感管理、任务查询及阶段 B 综合质量验收          | 已完成 | 管理端权限测试和阶段 B 全量验收通过                    |

### B1 验收条件

- [x] Prisma migration 建立灵感作品及发布状态模型，52 条原型基线数据可重复导入。
- [x] `GET /inspirations` 支持分类和关键词过滤，`GET /inspirations/:slug` 返回详情，具备 API 单元测试和数据库实测。
- [x] 用户端默认进入灵感目录，真实接入 API，支持分类、搜索、空状态、加载状态和失败恢复。
- [x] 列表保留原始图片比例，桌面多列、移动双列，无重复 ID、缺失 DOM 目标和控制台错误。
- [x] 浅色、深色、跟随系统、中英文及桌面/移动视口检查通过。

### B1 完成评估

评估日期：2026-08-03

| 检查项       | 结果 | 证据                                                                                     |
| ------------ | ---- | ---------------------------------------------------------------------------------------- |
| 数据模型     | 通过 | Prisma migration 已应用；AST 提取 52 条原型数据，连续两次 seed 后仍为 52 条              |
| 公开 API     | 通过 | 列表、分类、关键词、详情和非法分类已实测；API 2 个测试文件、4 个测试通过                 |
| 用户端目录   | 通过 | 首页跳转、52 条瀑布流、分类、搜索、加载、空状态、失败重试和详情页均连接真实 API          |
| 主题与语言   | 通过 | 浅色、深色、跟随系统及中英文切换均已在浏览器验证                                         |
| 响应式与 DOM | 通过 | 桌面 4 列、移动 2 列；详情页无重叠或横向溢出；无重复 ID、缺失图片、缺失目标或控制台错误  |
| 全量质量门禁 | 通过 | 格式、脚本语法、13 个 lint、13 个类型检查、13 个单元测试及 9 个生产构建任务全部通过      |
| 运行时       | 通过 | 用户端和管理端 HTTP 200，API 健康检查正常，Worker 已连接 Redis，组合查询精确返回目标作品 |

阶段结论：B1 验收条件全部达成，准许进入 B2。B2 已开始；阶段 B 继续保持“进行中”。

### B2 验收条件

- [x] Prisma migration 建立用户、验证码挑战、协议接受记录和安全会话模型。
- [x] API 支持发送演示验证码、协议校验、登录、会话查询和退出；验证码与会话令牌仅保存哈希。
- [x] 用户端提供手机号、验证码、倒计时、协议弹窗、错误状态和登录中状态。
- [x] 账户入口根据服务端会话显示访客、登录用户和退出操作，刷新后保持登录状态。
- [x] 受保护创作动作保存站内来源与草稿，跨刷新保留；登录后恢复且不自动提交。
- [x] CI 配置 PostgreSQL 17、`migrate deploy`、构建后 API 健康等待和认证 smoke 门禁。
- [x] 正式用户端以 `prototype/index.html` 和 `docs/phase-1` 为唯一设计基线，对齐导航、工具栏、瀑布流、详情复用输入器、账户菜单、登录页和响应式规则。
- [x] 桌面和移动端完成登录、刷新恢复、退出、主题及中英文浏览器 E2E，无重复 ID、缺失目标、溢出或控制台错误。
- [x] GitHub PR CI 实际运行并通过认证集成门禁。

### B2 当前评估

评估日期：2026-08-03

| 检查项       | 结果     | 证据                                                                                                                                                               |
| ------------ | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 数据模型     | 通过     | 认证 migration 已应用；只读数据库检查确认验证码和会话字段仅保存 64 位哈希                                                                                          |
| 服务端规则   | 通过     | 10 个 API 测试覆盖手机号、协议、错误验证码、令牌生成、会话查询和退出                                                                                               |
| HTTP 集成    | 通过     | `pnpm auth:smoke` 验证协议 400、错误码 401、登录 200、会话有效、退出 204 及立即失效                                                                                |
| 协议审计     | 通过     | 数据库存在版本 `2026-08-03` 的三协议完整接受记录                                                                                                                   |
| 意图恢复     | 通过     | 4 个用户端测试覆盖存储边界；浏览器实测登录返回详情后恢复 134 字草稿且不自动提交                                                                                    |
| 页面运行时   | 通过     | `/login`、灵感详情和管理端均返回 HTTP 200，API 与 Worker 正常启动                                                                                                  |
| 全量质量门禁 | 通过     | 格式、脚本语法、13 个 lint、13 个类型检查、12 个测试文件共 25 个测试及 9 个构建任务通过                                                                            |
| CI 集成配置  | 本机通过 | PostgreSQL 服务、`migrate deploy`、构建后 API 和认证 smoke 已配置，并在本机独立端口复演通过                                                                        |
| 原型视觉对齐 | 代码通过 | 正式页面直接采用原型结构、类名、视觉变量和 767/1199px 断点；移除非原型大标题、卡片详情和登录装饰                                                                   |
| 真实数据运行 | 通过     | PostgreSQL/Redis 物理机启动；52 条灵感 seed 成功；API、灵感页和登录页均返回 HTTP 200                                                                               |
| 浏览器 E2E   | 通过     | 1440x900 与 390x844 实测 52 卡片、0 坏图、无重复 ID/横向溢出/控制台错误；登录刷新退出、主题语言、详情创作器、参考图和意图恢复均通过；设置菜单显示创作额度 80 / 100 |
| 远端 CI      | 通过     | PR #4 的 `verify` 成功，run `30791545594`，认证集成门禁完整执行                                                                                                    |

当前结论：B2 的主体功能、API 集成、原型 1:1 对齐、浏览器 E2E 和远端 CI 均已通过。B2 标记为“已完成”，准许进入 B3；B3 尚未开始。

### B3 验收条件

- [x] Prisma migration 建立生成会话、任务参数快照、结果、持久化事件、额度账户和额度流水。
- [x] API 提供登录用户额度、会话列表/详情/重命名/删除、幂等任务提交、任务查询和取消。
- [x] 所有会话与任务读写按登录用户隔离，跨用户访问返回 404，匿名访问返回 401。
- [x] 额度在提交事务中预留，成功后消费，入队失败、生成失败或取消后返还；余额不足时禁止提交。
- [x] BullMQ Worker 原子领取任务，使用确定性模拟结果并写回原会话，重复 Job 不覆盖终态。
- [x] 任务事件持久化；SSE 支持 `Last-Event-ID`，断线后只重放游标之后的事件。
- [x] 状态机、成本、命名、尺寸、额度守恒、并发幂等、队列失败和 Worker 结果具备单元测试。
- [x] 物理机 PostgreSQL、Redis、API 和 Worker 完成成功、取消、额度结算、SSE 重放与数据隔离 smoke。
- [x] GitHub 堆叠 PR CI 实际运行并通过 PostgreSQL 17、Redis 7 和生成闭环门禁。

### B3 当前评估

评估日期：2026-08-03

| 检查项       | 结果 | 证据                                                                                                |
| ------------ | ---- | --------------------------------------------------------------------------------------------------- |
| 数据模型     | 通过 | 两个生成迁移均已应用；比例/清晰度使用数据库枚举，任务、结果、额度和流水具备 CHECK 约束              |
| 核心规则     | 通过 | 任务状态机、终态、2K/4K 成本、会话命名、输出尺寸及额度预留/消费/返还使用共享 `packages/core`        |
| API 与鉴权   | 通过 | 匿名额度 401；会话/任务按用户归属查询；跨用户任务返回 404；输入、额度不足和活动会话删除均有明确错误 |
| 幂等与补偿   | 通过 | 同参数重放与 P2002 并发冲突返回原任务；同键异参返回 409；入队失败返还额度，已入队回写失败不误退款   |
| Worker       | 通过 | BullMQ `image-generation` 队列实测排队 -> 生成中 -> 成功；取消任务不被结果覆盖                      |
| SSE          | 通过 | 事件表自增游标持久化；`Last-Event-ID` 单测及物理机 queued/generating/succeeded/cancelled 重放通过   |
| 额度守恒     | 通过 | 成功任务流水 RESERVE -> CONSUME；取消任务流水 RESERVE 2 -> RELEASE 2，最终 reserved=0               |
| 物理机闭环   | 通过 | 新幂等键实测成功、取消、同键重放、异参 409、结果、额度、SSE 和用户隔离；最终额度 93/100             |
| 全量质量门禁 | 通过 | 14 个 lint、14 个类型检查、18 个测试文件共 48 个测试及 9 个生产构建任务全部通过                     |
| 远端 CI      | 通过 | PR #6 `verify` 成功，run `30796305017`；PostgreSQL 17、Redis 7、API/Worker 与两个 smoke 均通过      |

当前结论：B3 的数据、API、Worker、额度、SSE、本机闭环和远端 CI 证据齐全，阶段标记为“已完成”，准许进入 B4 用户端生成界面与管理端联通。

### B4 验收条件

- [x] `/generate` 和 `/generate/:sessionId` 使用正式用户端导航壳，受保护路由能保存并恢复登录前创作草稿。
- [x] 会话列表、会话详情、重命名、删除、任务提交、任务取消、额度和 SSE 进度全部调用真实 API。
- [x] 新对话在首次提交前不创建数据库记录，提交后使用服务端返回的会话 ID 跳转。
- [x] 工作台支持提示词、参考图、模型、比例、2K/4K、数量、成本预估及额度不足拦截。
- [x] 支持任务状态、失败信息、结果预览、下载、修改提示词、再次生成和筛选搜索。
- [x] 统一 `EXTERNAL_SERVICES_MODE=mock|live`；mock 由服务端模拟外部响应，live 缺少适配器时快速失败且不调用第三方。
- [x] 桌面、中等宽度和手机布局无重复 ID、横向溢出、左栏覆盖或工具栏内容重叠。
- [x] 全量本地质量门禁通过。
- [x] 堆叠 PR 远端 CI 通过。

### B4 当前评估

评估日期：2026-08-03

| 检查项       | 结果 | 证据                                                                                                   |
| ------------ | ---- | ------------------------------------------------------------------------------------------------------ |
| 原型结构     | 通过 | 生成侧栏、紧凑搜索、三项筛选、空会话引导、推荐提示词、输入器和参数面板按 `prototype/index.html` 实现   |
| API 联通     | 通过 | 浏览器提交后进入真实会话 URL，Worker 返回 2 张模拟结果；会话删除确认与清理均调用 API                   |
| 外部服务开关 | 通过 | 配置、认证验证码、参考图模拟和 Worker 共享 `EXTERNAL_SERVICES_MODE`；live 未配置供应商时拒绝启动或请求 |
| 响应式       | 通过 | 902×857 和 390×844 实测；搜索默认宽 74px 并可展开，手机输入器左右 12px，无横向溢出或重复 ID            |
| 工程检查     | 通过 | 14 个 lint、14 个类型检查、18 个测试文件共 49 个测试及 9 个生产构建通过；两个生成路由已产出            |
| 远端 CI      | 通过 | PR #8 `verify` 成功，run `30804763044`；会话动态宽度修复提交 `fd606dd` 全部门禁通过                    |

当前结论：B4 的功能、真实 API 联调、原型对齐、本地全量门禁和远端 CI 证据齐全，阶段标记为“已完成”，准许进入 B5。PR #8 仍保持开启，未经用户明确批准不合并。

### B5 验收条件

- [x] 管理端使用独立登录页、独立管理员会话 Cookie 和退出流程，不复用普通用户会话。
- [x] API 对每个管理端请求执行 RBAC；匿名、普通用户和权限不足的管理员分别返回明确的 401/403。
- [x] 管理端任务页通过真实 API 查询生成任务，支持状态、模型、时间和关键词筛选、分页及详情查看。
- [x] 管理端灵感页通过真实 API 创建、修改、发布/下架灵感，字段校验和发布状态由 API 保证。
- [x] 管理端提供稳定的工作台布局、导航、加载、空、错误和无权限状态，在桌面和移动视口无重叠或横向溢出。
- [x] 管理员认证、会话过期、RBAC、任务查询、灵感写入和跨角色访问具备 API 测试与物理机 smoke 证据。
- [x] 阶段 B 的用户端、管理端、API、Worker、PostgreSQL 和 Redis 闭环完成全量本地检查及远端 CI。

### B5 当前评估

评估日期：2026-08-03

| 检查项          | 结果 | 证据                                                                                                         |
| --------------- | ---- | ------------------------------------------------------------------------------------------------------------ |
| 阶段准入        | 通过 | B4 本地全量门禁与 PR #8 远端 `verify` 通过                                                                   |
| 独立管理员认证  | 通过 | `AdminUser`/`AdminVerificationCode`/`AdminSession` 与 `dreamspace_admin_session` Cookie 不复用普通用户会话   |
| API RBAC        | 通过 | 任务和灵感接口统一经 `AdminPermissionGuard` 检查读/写权限；smoke 覆盖匿名 401、普通用户 401 和审阅员写入 403 |
| 任务查询闭环    | 通过 | 真实 API 支持状态、模型、日期、关键词、分页和详情；结束日期包含当日全部数据，结果图在管理端正常加载          |
| 灵感写操作      | 通过 | 真实 API 支持创建草稿、编辑、发布/下架和字段校验；公开 API 可见性实测为 404 → 200 → 404                      |
| 物理机 smoke    | 通过 | `pnpm admin:smoke`：认证隔离、任务查询、灵感 CRUD、校验 400、审阅员读 200/写 403、退出后 401                 |
| 页面与响应式    | 通过 | 1280×720 和 390×844 浏览器实测；任务详情、灵感列表/编辑抽屉、只读权限态正常，无重复 ID 或水平溢出            |
| 本地全量门禁    | 通过 | 14 项 lint、14 项类型检查、24 个测试文件共 71 项测试、9 项生产构建通过                                       |
| 第一切片远端 CI | 通过 | PR #10 `verify` 通过，run `30808166935`，耗时 2 分 13 秒                                                     |
| 第二切片远端 CI | 通过 | PR #12 `verify` 通过，run `30813263162`，耗时 2 分 46 秒                                                     |
| 综合浏览器 E2E  | 通过 | 5 条 Playwright 用例覆盖用户生成/会话、管理员写操作、Viewer 只读、三种主题、中英文及桌面/移动布局            |
| 恢复与草稿      | 通过 | 首个 SSE 流主动中断后自动重连并取得 Worker 结果；两个会话草稿经真实 API 切换、刷新后独立恢复                 |
| 并发与响应式    | 通过 | 10 个全新用户的 20 个首次额度并发请求均为 200；1280px 英文工具栏和 390px 移动端无水平溢出                    |
| 最终远端 CI     | 通过 | PR #12 `verify` 通过，run `30817677831`，耗时 3 分 48 秒；CI 内 5 条 E2E 全部通过                            |

当前结论：B5 功能、权限、完整栈闭环、恢复行为、响应式及本地/远端质量门禁均已通过，标记为“已完成”。PR #12 仍保持开启，未经用户明确批准不合并。

### 阶段 B 完成评估

- [x] 核心流程具备 API 集成测试和 Playwright E2E。
- [x] 登录意图、会话草稿和 SSE 断线恢复符合产品文档。
- [x] 浅色、深色、跟随系统、中英文、桌面和移动布局检查通过。
- [x] JavaScript 语法、重复 ID、缺失 DOM 目标和控制台错误检查通过。

阶段结论：B1-B5 的用户端、管理端、API、Worker、PostgreSQL 和 Redis 目标均有本地与远端证据，阶段 B 标记为“已完成”。已具备进入阶段 C 的准入条件，阶段 C 仍为“未开始”。

## 阶段 C：真实生成能力

### 目标

接入对象存储、上传、真实模型供应商、生成前后审核、图片处理、额度冻结/结算、失败重试、死信队列和定时对账。

### 子阶段

| 子阶段 | 可验证目标                                          | 状态   | 下一子阶段准入条件                          |
| ------ | --------------------------------------------------- | ------ | ------------------------------------------- |
| C1     | 真实文件上传、解码清洗、mock 存储边界和数据归属校验 | 已完成 | 上传、读取、跨用户拒绝和生成链路验收通过    |
| C2     | S3 兼容对象存储、签名访问、生成结果落库与缩略图处理 | 已完成 | MinIO 物理机验收与对象归属测试通过          |
| C3     | 可替换真实模型适配器、输入/输出审核、回调验签和幂等 | 阻塞   | 完成 C3a 验收，并确定供应商、凭据和回调协议 |
| C4     | 可分类重试、死信队列、取消、额度结算和定时对账补偿  | 进行中 | C4a 验收通过，C4b 定时对账与补偿验收通过    |
| C5a    | 多实例 Worker、对账接管、队列恢复和一致性故障演练   | 未开始 | 供应商无关的生产可靠性演练通过              |
| C5b    | 真实供应商完整栈安全、可靠性、成本和故障恢复验收    | 阻塞   | C3b 完成且预发布环境可用                    |

### C1 验收条件

- [x] 登录用户通过 multipart 上传真实 JPG、PNG 或 WebP，服务端不信任扩展名或声明类型。
- [x] 图片经过解码、像素上限、单帧校验、方向纠正、元数据清除和 WebP 统一编码。
- [x] mock 模式使用原子写入的本地存储适配器；live 模式缺少真实适配器时快速失败。
- [x] PostgreSQL 保存对象键、原文件名、尺寸、字节数、SHA-256 和用户归属。
- [x] 文件读取与生成任务提交均校验当前用户归属，不存在返回 404 以避免枚举。
- [x] 用户端上传点改为真实文件 API，不再只上报文件名和大小。

### C1 完成评估

| 检查项         | 结果 | 证据                                                                               |
| -------------- | ---- | ---------------------------------------------------------------------------------- |
| 图片清洗与存储 | 通过 | 真实 WebP 上传后输出 1080×720、33,802 字节，下载文件 SHA-256 与 API 返回值一致     |
| 访问与归属     | 通过 | 文件所有者读取 200，匿名读取 401，其他用户读取 404，他人参考图创建任务 400         |
| 生成链路回归   | 通过 | 所有者携带参考图创建任务后 Worker 返回 `succeeded`，额度从预留进入消费             |
| API 自动化测试 | 通过 | 10 个测试文件、41 项测试通过，包含解码清洗、类型伪造、归属、原子写入和路径穿越检查 |

当前结论：C1 标记为“已完成”，阶段 C 仍为“进行中”。C2 可开始实现，但必须保留 mock 适配器供本地和 CI 使用，live 对象存储不得回退到本地目录。

### C2 验收条件

- [x] API 与 Worker 共用 `@dream-space/storage` 边界，支持原子本地文件与 S3/MinIO 两种实现。
- [x] `OBJECT_STORAGE_MODE=local|s3` 与外围服务模拟开关解耦；S3 模式连接失败时不回退本地目录。
- [x] Worker 将供应商原始字节按任务比例和分辨率统一转换为 WebP，并生成最大宽度 480px 的 WebP 缩略图。
- [x] PostgreSQL 保存原图键、缩略图键、SHA-256、原图/缩略图尺寸和字节数，并以约束拒绝不完整元数据组。
- [x] 用户端按结果归属、管理端按 `tasks:read` 权限访问资产；本地模式返回受控字节流，S3 模式返回 300 秒签名地址。
- [x] 对象写入中途失败、数据库拒绝终态或落库异常时删除已写对象，任务失败后释放预留额度。
- [x] macOS 物理机 MinIO 可由项目脚本启动，自动创建隔离 bucket，不依赖 Docker。

### C2 本地完成评估

| 检查项         | 结果 | 证据                                                                                                |
| -------------- | ---- | --------------------------------------------------------------------------------------------------- |
| 存储适配器     | 通过 | 本地原子读写、路径穿越拒绝；S3 put/get/delete、键白名单和 300 秒签名测试通过                        |
| 图片处理与落库 | 通过 | 物理机任务产出原图 2048×2048/160,056 字节、缩略图 480×480/17,010 字节，数据库 SHA-256 长度 64       |
| 数据完整性     | 通过 | 两次迁移成功；事务探针确认数据库拒绝缺少 checksum/缩略图元数据的对象结果                            |
| 用户端访问     | 通过 | local 模式原图/缩略图为 200，S3 模式为 302 且签名 TTL 300；匿名 401、跨用户结果 404                 |
| 管理端访问     | 通过 | 管理员任务详情及原图/缩略图在 local 为 200、S3 为 302；普通用户访问管理接口 401，Viewer 写入 403    |
| 失败补偿       | 通过 | Worker 自动化测试确认数据库忽略结果时删除原图和缩略图；供应商失败进入失败结算路径                   |
| 物理机 MinIO   | 通过 | Homebrew MinIO/mc、9000/9001、`dreamspace-local` bucket；参考图、原图、缩略图实体与数据库字节数一致 |
| 本地全量门禁   | 通过 | `pnpm check`：16 项 lint、16 项类型检查、28 个测试文件共 88 项测试、10 项生产构建通过               |
| 远端 CI        | 通过 | PR #16 `verify` 通过，run `30871573193`，耗时 3 分 56 秒                                            |

当前结论：C2 的实现、自动化测试、本地与 S3 双模式 smoke、数据库约束、物理机 MinIO 和远端 CI 均已通过，标记为“已完成”。C3 可以开始适配器和审核边界设计；接入沙箱的准入条件仍是确定真实模型供应商与测试凭据。

### C3a：供应商与审核边界

本切片不臆造具体供应商 SDK、请求字段或回调协议。在供应商尚未确定前，先完成与供应商无关的边界和可验证的 mock 行为：

- [x] Worker 通过 `GenerationProvider` 接口隔离模型调用；mock 模式使用确定性图片，live 模式未配置适配器时快速失败。
- [x] Worker 在模型调用前后执行 `ContentModerator`，输入拒绝不会调用模型，输出拒绝不会写入对象存储。
- [x] PostgreSQL 持久化任务输入/输出审核状态和结果审核状态；审核事件保存 decision 与 codes。
- [x] 结果对象写入前必须通过输出审核；数据库约束禁止未审核通过的结果持有对象键。
- [x] 回调验签纯函数使用原始请求体、时间戳和 HMAC-SHA256，并拒绝过期、篡改和畸形签名。
- [x] Worker 单元测试覆盖正常审核、输入拒绝、输出拒绝、供应商失败和对象补偿路径；API/管理端返回并展示审核状态。

#### C3a 完成评估

评估日期：2026-08-05

| 检查项       | 结果 | 证据                                                                                  |
| ------------ | ---- | ------------------------------------------------------------------------------------- |
| 类型检查     | 通过 | `packages/core`、`apps/worker`、`apps/api`、`apps/admin` 的 `tsc --noEmit` 均通过     |
| 回调验签     | 通过 | `packages/core/test/provider-callback.test.ts` 覆盖原始体、过期、篡改和畸形签名       |
| Worker 审核  | 通过 | `apps/worker` 9 项测试通过，覆盖输入/输出拒绝、额度返还、供应商失败和对象补偿         |
| 数据模型     | 通过 | moderation migration 增加三组状态字段、历史回填及对象审核约束                         |
| API/管理端   | 通过 | 任务和结果契约映射审核状态，管理端详情展示输入/输出审核状态；SSE 契约覆盖两种审核事件 |
| 物理机闭环   | 通过 | 成功生成 smoke 与输入拒绝 smoke 均通过；拒绝任务额度 14 -> 14，SSE 审核事件可回放     |
| 全量质量门禁 | 通过 | 10 项 lint、10 项类型检查、29 个测试文件共 93 项测试及 10 项生产构建通过              |

当前结论：C3a 的供应商隔离、审核边界、状态持久化、回调验签、SSE 事件和物理机闭环已完成，具备用户验收条件；C3 仍不得标记完成。C3b 准入条件为：确定具体模型供应商、取得不写入仓库的沙箱凭据、获得正式请求/回调字段协议，并完成沙箱端到端生成与幂等回调复演。

### C4a：分类重试与持久化死信

本切片只处理任务重试、最终失败结算和死信可观测性。定时扫描、差异识别和自动补偿属于 C4b；C4a 完成不代表整个 C4 完成。

- [x] 供应商错误区分可重试与不可重试；不可重试错误立即失败并保留供应商错误码。
- [x] BullMQ 使用指数退避执行最多三次尝试；中间失败不修改额度账户和额度流水。
- [x] PostgreSQL 持久化执行次数和最近尝试键；相同尝试键的重复投递不会重复执行或重复计数。
- [x] 可重试错误耗尽次数后，在同一事务内将任务标记失败、写入死信、释放额度并写入死信/失败事件。
- [x] 失败结算使用幂等额度流水且仅释放一次；管理端任务详情展示执行次数和死信记录。
- [x] 自动化测试覆盖重试中间态、重试耗尽、不可重试错误、重复尝试键和原子死信结算。
- [x] 物理机 PostgreSQL、Redis、API 和 Worker 完成重试成功、重试耗尽及 Worker 重启恢复演练。

#### C4a 当前评估

评估日期：2026-08-06

| 检查项         | 结果 | 证据                                                                                              |
| -------------- | ---- | ------------------------------------------------------------------------------------------------- |
| 错误分类       | 通过 | 可重试错误交还 BullMQ；不可重试错误首次执行即失败，保留错误码且不写死信                           |
| 尝试幂等       | 通过 | 每次领取原子增加执行次数并记录尝试键；重复尝试键返回忽略，不产生第二次事件或额度结算              |
| 死信与额度     | 通过 | 重试耗尽在单一事务中写任务终态、死信、RELEASE 流水和事件；数据库约束拒绝非正数死信次数            |
| 物理机故障演练 | 通过 | 一次瞬时故障在第 2 次成功；持续故障第 3 次进入死信；Worker 重启后继续 Redis 重试并在第 2 次成功   |
| SSE 与管理端   | 通过 | SSE 可见 `task.retrying`、`task.dead_lettered`；任务详情显示执行 3 次及死信错误码                 |
| 响应式页面     | 通过 | 管理端详情在 902×858 与 390×844 无横向溢出或重叠，移动端死信信息完整可见，浏览器控制台无错误      |
| 全量质量门禁   | 通过 | Prisma、格式和脚本检查；10 项 lint、10 项类型检查、30 个测试文件共 100 项测试及 10 项生产构建通过 |

当前结论：C4a 实现、专项自测和用户验收均已完成，准许提交 PR。C4 保持“进行中”；C4b 仍需实现定时对账、差异检测、幂等补偿和恢复演练。

### C4b：定时对账与幂等补偿

本切片以额度流水为账务真相来源。任务数据只用于发现缺失结算和证明补偿是否安全；无法证明的差异必须阻塞并交由人工处理，禁止直接改余额。

- [x] Worker 按可配置时间窗口执行额度对账，同一窗口只允许一个运行记录。
- [x] 识别缺失预留、消费、释放、结算金额错误及账户总额、可用额、预留额漂移。
- [x] 自动补偿缺失消费流水，以及能证明属于终态任务且不会侵占活跃任务预留的退款。
- [x] 金额冲突、账户漂移和归属不确定的退款记录为 `BLOCKED`，不猜测修复。
- [x] 管理 API 和任务页展示最近对账扫描数、差异数、补偿数及待处理数。
- [x] 单元、API、smoke、物理机数据库和浏览器 E2E 自测通过。
- [x] 用户确认降低交互优先级，并要求当前实现直接进入 PR review。
- [x] 分支提交、推送并创建 PR。
- [ ] 远端 CI 和 PR review 通过。

#### C4b 当前评估

评估日期：2026-08-07

| 检查项       | 结果   | 证据                                                                           |
| ------------ | ------ | ------------------------------------------------------------------------------ |
| 账务模型     | 通过   | `GRANT/RESERVE/CONSUME/RELEASE` 流水推导账户；删除业务任务不会制造伪差异       |
| 并发幂等     | 通过   | 唯一时间窗口阻止多 Worker 重复补偿；同窗口重跑不新增 `RELEASE`                 |
| 安全补偿     | 通过   | 失败任务缺失退款从 95/5 恢复为 100/0；不确定差异保持 `BLOCKED`                 |
| 自动化测试   | 通过   | Worker 5 个文件 17 项、API 11 个文件 49 项通过，相关类型检查通过               |
| 物理机演练   | 通过   | 12 个迁移已应用；对账 smoke、管理 smoke 和只读流水公式核对通过，探针数据已清理 |
| 浏览器 E2E   | 通过   | 管理员与 Viewer 两条用例通过；修复异步列表尚未加载时提前计数导致的测试竞态     |
| 全量质量门禁 | 通过   | 16 项 lint、16 项类型检查、31 个测试文件 104 项测试及 10 项生产构建通过        |
| PR review    | 进行中 | 已按用户 2026-08-14 的明确要求创建 PR #20，等待远端 CI 和 review               |

当前结论：C4b 实现和本地自测已完成并提交 PR #20，等待远端 CI 和 review。C4 在 PR 通过前继续标记为“进行中”。多实例崩溃后的租约接管属于 C5 商用可靠性演练，不由单窗口去重替代。

根据 2026-08-14 的优先级调整，C4b 的交互和运营优化降为 P2；保留后台运行、账务安全和人工阻塞项能力，不再把它作为管理端近期主路径。当前已完成代码按用户要求直接进入 PR review，不等待单独的对账体验验收。

### C5：阶段 C 生产级可靠性验收

C5 分两条依赖线推进，避免真实供应商阻塞所有可靠性工作：

- C5a（供应商无关）：至少两个 Worker 并发运行，对账使用可超时租约或等价机制；演练领取实例崩溃、租约接管、重复投递、Redis 重启、对象写入失败和数据库短暂不可用，最终任务、额度流水和对象引用收敛一致。
- C5b（真实供应商）：在 C3b 和预发布环境完成后，演练真实生成、输入/输出审核、超时、限流、回调重放、供应商故障、降级和成本上限。

C5a 可以在 C3b 阻塞期间先行实现；C5b 未完成前，阶段 C 不得标记“已完成”。两条线都必须具备自动化测试、物理机或预发布故障记录、恢复时长和远端 CI 证据。

### 验收条件

- 供应商适配器可替换，业务层不依赖具体 SDK。
- 任务状态迁移、回调验签、幂等、取消和重试测试通过。
- 输入与输出审核、文件清洗、签名下载和数据归属检查通过。
- 供应商故障、Worker 重启和对账补偿演练通过。

## 阶段 D：运营与上线

### 目标

完善用户与额度、审核申诉、模型路由、系统配置、审计日志、监控告警、备份恢复、部署和合规能力。

### 管理端近期主线

管理端不是阶段 B5 的“登录、灵感、任务”MVP 的简单扩展，而是独立的运营产品。详细需求和架构分别见 [管理端产品需求](admin-console-requirements.md) 与 [管理端功能架构](admin-console-architecture.md)。

| 切片  | 目标                                          | 优先级 | 当前状态               | 准入证据                                    |
| ----- | --------------------------------------------- | ------ | ---------------------- | ------------------------------------------- |
| ADM-0 | 管理端壳、通用列表/详情、权限上下文和异常状态 | P0     | 基础已完成，需补齐     | 路由守卫、移动/桌面 E2E、统一错误与无权限态 |
| ADM-1 | 工作台、用户、任务、灵感和图片资产基础管理    | P0     | 任务/灵感部分完成      | 每个列表真实 API、分页、详情和写操作 smoke  |
| ADM-2 | 管理员账号、角色权限、临时授权和审计日志      | P0     | 登录/RBAC 部分完成     | 角色矩阵、越权测试、审计查询                |
| ADM-3 | 自动/人工图片审核、下架恢复和申诉             | P0     | 边界已完成，后台未开始 | 审核队列、双人审批、申诉全链路              |
| ADM-4 | 模型、供应商、路由、灰度、参数和成本管理      | P0     | 未开始                 | 草稿发布、健康检查、回滚和降级              |
| ADM-5 | 系统配置、隐私请求、指标告警和授权台账        | P1     | 未开始                 | 告警和数据请求演练                          |
| ADM-6 | 额度对账和备份恢复运营增强                    | P2/P0  | 对账已完成，恢复未开始 | 对账补偿、RPO/RTO 和恢复演练                |

ADM-0 至 ADM-4 是下一阶段主线；对账不再阻塞管理端基础能力建设，但对账的账务安全约束必须保留。

管理端切片采用统一门禁：

| 门禁 | 必须回答的问题                                     | 证据                                   |
| ---- | -------------------------------------------------- | -------------------------------------- |
| 进入 | 数据归属、权限动作、状态迁移和失败补偿是否明确？   | 需求条目、API 契约、迁移草案、风险记录 |
| 实现 | 所有按钮是否调用真实 API，是否有幂等键和审计字段？ | 服务/控制器、权限测试、审计样例        |
| 自测 | 正常、空、错误、越权、重复提交和移动端是否可复现？ | 单测、smoke、Playwright、响应式检查    |
| 验收 | 用户能否按目标角色完成完整工作流？                 | 角色验收记录和缺陷清单                 |
| 退出 | 是否具备回滚/禁用开关，文档和代码状态是否一致？    | 发布说明、回滚演练、计划更新           |

### 子阶段

| 子阶段      | 可验证目标                                               | 状态   | 下一子阶段准入条件                   |
| ----------- | -------------------------------------------------------- | ------ | ------------------------------------ |
| D1 安全基线 | 限流、安全响应头、CORS/CSRF、会话、密钥和依赖安全        | 未开始 | 安全自动化、配置扫描和攻击面检查通过 |
| D2 运营治理 | 高风险操作审计、审核申诉、隐私请求、留存删除和授权台账   | 未开始 | 关键运营动作可追溯且流程可执行       |
| D3 可观测性 | 结构化日志、请求 ID、指标、追踪、看板、告警和值班手册    | 未开始 | 故障注入可由告警发现并定位           |
| D4 数据保护 | PostgreSQL/对象存储备份、恢复、一致性校验和 RPO/RTO      | 未开始 | 独立环境完整恢复演练通过             |
| D5 生产发布 | 正式拓扑、环境隔离、迁移、灰度、回滚、密钥轮换和灾难手册 | 未开始 | 预发布发布及回滚演练通过             |
| D6 上线验收 | 负载、容量、成本、供应商限额、安全和跨团队上线评审       | 未开始 | P0 风险清零并批准生产放量            |

### D1 验收条件

- API 具备按身份、IP 和高成本动作分层限流，登录、验证码、上传和生成不能被绕过。
- 安全响应头、HTTPS、可信代理、CORS 白名单、Cookie 和 CSRF 策略有代码及代理层双重验证。
- 生产 Secret 使用密钥管理服务，完成最小权限、轮换和泄漏应急演练。
- SAST、依赖、镜像或主机漏洞扫描以及上传攻击面测试进入 CI/发布门禁。

### D2 验收条件

- 登录、权限、额度调整、内容发布、审核处置和系统配置等高风险操作写入不可抵赖审计日志。
- 管理端支持审核申诉、人工复核和双人审批需要的状态与证据链。
- 明确数据分类、留存期限、账户删除、数据导出和隐私请求处理流程。
- 灵感素材、字体、提示词、用户上传和生成结果具备授权、投诉及下架流程。

### D3 验收条件

- Web、Admin、API 和 Worker 使用关联请求 ID 的结构化日志，敏感数据完成脱敏。
- API 延迟/错误、队列积压、任务成功率、供应商错误、对象存储和额度差异具备指标与看板。
- 核心链路具备跨 API、队列、Worker 和供应商的追踪，告警包含可执行处置手册。
- 通过故障注入验证告警到达、定位和升级路径，记录恢复时间。

### D4 验收条件

- PostgreSQL 执行自动备份与时间点恢复，对象存储开启版本或等价保护；Redis 不作为唯一业务事实来源。
- 在隔离环境恢复数据库与对象，校验任务、额度流水和对象引用一致性。
- RPO/RTO 经业务批准并通过演练；备份加密、保留、访问和删除满足数据策略。

### D5 验收条件

- 开发、CI、预发布和生产环境隔离，生产使用托管 PostgreSQL、Redis 和对象存储。
- 发布采用不可变构建产物、迁移前置检查、健康/就绪探针、灰度放量和自动停止条件。
- 数据库变更遵循 expand-contract；应用回滚、前向修复和密钥轮换均完成演练。
- 多实例 Worker 和对账任务完成租约、崩溃接管、重复投递和网络分区演练。

### D6 验收条件

- 预发布环境完成真实供应商全链路、峰值负载、限额、降级和成本预算测试。
- 内部 SLO、容量阈值、供应商依赖目标和首批放量规模有测量依据并获批准。
- [商用就绪审计](commercial-readiness.md) 中 P0 风险全部关闭，P1 风险有负责人和期限。
- 产品、运营、研发、安全和合规完成上线清单评审，回滚负责人和沟通路径明确。

支付、会员、充值、订单、退款和发票不属于阶段 D。产品决定收费时必须新增独立商业化阶段，并重新评估账务、支付安全和合规范围。

## 评估规则

- 每个阶段结束时逐项更新验收条件，不用主观百分比代替证据。
- 存在阻断性未完成项时，阶段不得标记为“已完成”。
- 非阻断问题必须记录负责人、处理阶段和风险，不得静默带入下一阶段。
- 每次评估记录对应分支、提交、检查命令和结果，便于复核与回滚。
